import React from 'react';
import {Text, View} from 'react-native';

const WeatherForDayScreen = props => {
  console.log(props);
  return (
    <View>
      <Text>WeatherForDayScreen</Text>
    </View>
  );
};

export default WeatherForDayScreen;
